## 1. 页面画布与页头布局

- [x] 1.1 在 `DownloadPage` 中增加局部响应式页面 padding、居中的 `max-w-6xl` 内容容器和底部留白，确保不修改 `App.tsx` 的全局 keep-alive 页面容器。
- [x] 1.2 将页头重组为“标题 + 可换行全局操作组”的主工具栏，以及按条件渲染的下载目录次级信息行。
- [x] 1.3 为长目录路径保留 `title` 完整提示和截断能力，保证“打开”按钮不可压缩，并检查最小窗口宽度下没有横向溢出。

## 2. 任务列表视觉节奏

- [x] 2.1 将顶层任务列表间距统一为约 16px，并把普通任务卡与专辑卡统一为约 20px 内边距、圆角、背景、细边框和阴影；保留专辑 accent 左边框。
- [x] 2.2 调整展开章节子行的间距与内边距，使标题、操作按钮和进度条不贴边，同时保持章节操作逻辑不变。
- [x] 2.3 将“暂无下载任务”包装为与任务卡一致的空状态面板，并保持页头到内容区的统一垂直节奏。

## 3. 自动化回归验证

- [x] 3.1 更新 `tests/unit/pages/DownloadPage.test.tsx`，覆盖页面画布、分层页头、目录存在或缺失、任务列表和空状态的关键语义结构，避免断言完整 class 字符串。
- [x] 3.2 复核并运行 DownloadPage 定向测试，确认筛选、刷新、打开目录、暂停或恢复、取消或重试以及专辑折叠交互仍调用原有逻辑。
- [x] 3.3 运行 `npx tsc --noEmit`、`npm test` 和 `npm run lint`，修复本变更引入的类型、测试或 lint 回归。

## 4. 视觉验收

- [x] 4.1 在常规窗口与应用最小 900px 窗口下检查下载页，确认桌面约 24px、窄窗口至少 16px 的页面留白，工具栏可换行且无水平滚动。
- [x] 4.2 分别检查空队列、独立任务、专辑折叠和专辑展开状态，确认卡片间距、章节子行、长目录截断和操作按钮均清晰可用。
- [x] 4.3 在浅色、深色及 reduced-motion 设置下检查页面，确认边框层级清晰且现有动画降级语义未改变。
