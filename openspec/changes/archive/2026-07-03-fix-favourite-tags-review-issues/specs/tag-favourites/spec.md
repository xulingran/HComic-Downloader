## 修改需求

### 需求:用户必须能从详情抽屉的 tag chip 加入推荐标签

详情抽屉（`ComicInfoDrawer`）的 tag chip 小按钮点击后，必须弹出操作菜单，允许用户将当前标签加入当前来源的 `my_tags`（推荐）或 `tag_blacklist`（屏蔽）。弹窗选项必须根据标签当前状态动态展示，禁止平铺全部选项造成认知负担。

**「加入推荐 / 取消推荐」动作必须受来源能力门控**：仅当当前来源 `sourceSupportsTagRecommendation(comicSource)` 为真时，才可生成推荐相关动作（`favourite` / `unfavourite`）。不支持推荐的来源（如 NH、copymanga，即 `SOURCE_META[<source>].supportsTagRecommendation === false`）禁止暴露任何推荐入口，该来源下 tag chip 小按钮必须退化为仅「屏蔽 / 取消屏蔽」动作。这是与搜索页高亮门控、设置页来源 tab 门控一致的入口约束，禁止让用户对一个写入后无法生效、无法在设置页管理、重启后会被后端 normalize 丢弃的动作产生"假成功"。

#### 场景:未设置的标签弹窗展示加入推荐与屏蔽

- **当** 用户在支持推荐的来源下点击一个既不在 `my_tags` 也不在 `tag_blacklist` 的标签的小按钮
- **那么** 弹窗必须展示「加入推荐标签」与「加入屏蔽标签」两个选项
- **且** 用户选择「加入推荐标签」后，该标签必须加入 `my_tags[当前来源]`

#### 场景:已推荐的标签弹窗展示取消推荐

- **当** 用户在支持推荐的来源下点击一个已在 `my_tags`（推荐态）的标签的小按钮
- **那么** 弹窗必须展示「取消推荐标签」选项（以及「加入屏蔽标签」选项，若需切换）
- **且** 用户选择「取消推荐标签」后，该标签必须从 `my_tags[当前来源]` 移除

#### 场景:已屏蔽的标签弹窗展示取消屏蔽

- **当** 用户点击一个已在 `tag_blacklist`（屏蔽态）的标签的小按钮
- **那么** 弹窗必须展示「取消屏蔽标签」选项
- **且** 用户选择「取消屏蔽」后，该标签必须从 `tag_blacklist[当前来源]` 移除

#### 场景:详情抽屉 tag chip 的推荐态视觉

- **当** 详情抽屉展示的某标签已在 `my_tags[当前来源]` 中（大小写不敏感）
- **那么** 该 tag chip 必须应用推荐态样式（与现有 `recommendedTagSet` 命中样式一致，琥珀色）
- **且** 推荐态与屏蔽态互斥，已屏蔽的标签不显示推荐态

#### 场景:不支持推荐的来源禁止出现推荐入口

- **当** 详情抽屉展示的漫画来源为 `nh`（或任何 `SOURCE_META[<source>].supportsTagRecommendation === false` 的来源）
- **且** 用户点击该来源下任一 tag chip 的小按钮
- **那么** 弹窗 / 按钮动作必须只包含「屏蔽 / 取消屏蔽」，禁止出现「加入推荐」或「取消推荐」
- **且** 禁止对 `my_tags['nh']` 产生任何写入（既不通过入口动作，也不通过默认 `favourite` 分支）

#### 场景:支持推荐的来源不受门控影响

- **当** 详情抽屉展示的漫画来源为 `hcomic` / `moeimg` / `jm` / `bika`（`supportsTagRecommendation === true`）
- **那么** 推荐入口行为必须与门控引入前完全一致（加入 / 取消推荐照常可用）
