## 1. 共享漫画详情体验

- [x] 1.1 用现有 `ComicInfoDrawer` 定向测试补齐详情补全成功/空结果/异常重试、收藏提交、替换式搜索、追加标签搜索、标签推荐与屏蔽确认等当前行为基线
- [x] 1.2 从 `ComicInfoDrawer` 抽取可复用的详情状态 controller，接收 `comic`、`active` 和搜索表面回调，并统一管理补全、收藏、标签操作、确认状态及反馈计时器
- [x] 1.3 抽取共享详情内容组件，完整承载标题、作者、信息、分类、原网页、收藏、原著、角色、制作组、标签及加载/错误 UI，并支持 `drawer` / `reader` surface 样式
- [x] 1.4 将 `ComicInfoDrawer` 改为共享详情体验的抽屉外壳消费者，保留遮罩、右滑动画、关闭栏和原有搜索关闭语义，运行抽屉测试确认无回归

## 2. 详情尾页导航模型

- [x] 2.1 在 reader 纯函数层增加详情尾页导航模型，分别计算详情页位、最后图片 spread、从图片进入详情及从详情返回图片的目标
- [x] 2.2 为单页、双页奇偶页数以及 `blankPosition=none/front/end` 增加纯函数单测，确认详情不会被 step=2 越过且返回目标包含最后一张图片
- [x] 2.3 扩展 `PageFlipView` 支持可选尾部内容插槽，保持 `imageUrls` 仅含真实图片，并让详情在单页/双页下独占整个视口
- [x] 2.4 详情激活时关闭左右点击翻页覆盖层，加入可访问的“返回末页”操作，并实现详情内部 wheel 分流：可滚动时消费、顶部向上时返回、底部向下时保持终点
- [x] 2.5 让图片与详情之间的前进/后退继续经过既有方向动画、`isFlipping` 门控和硬上限自愈，增加动画期间重复输入被丢弃的测试

## 3. 在线阅读器尾页与章节边界

- [x] 3.1 在 `ComicReaderModal` 中区分真实图片总数、可选详情页位和导航终点，禁止向 `imageUrls` 追加哨兵 URL
- [x] 3.2 由 `selectedChapterId` 与章节列表可靠派生当前章节索引，覆盖历史记录直达末章，并只为单本或末章启用详情尾页
- [x] 3.3 调整键盘、滚轮、点击和章节边界路由：前置章节保持二次翻页进入下一章，末章最后图片继续前进进入详情且不显示下一章提示
- [x] 3.4 创建在线阅读器详情尾页容器，接入共享详情体验；替换式搜索必须关闭阅读器，追加标签搜索必须保留阅读器并显示反馈

## 4. 三种显示模式、进度与可见性

- [x] 4.1 在连续滚动图片列表末尾追加详情容器并注册到页面 refs，使可见性追踪与进度拖动可以定位详情，同时保持详情宽度独立于 `imageWidth`
- [x] 4.2 为连续滚动详情增加可见性激活门控，确保详情未进入或接近视口时不提前执行详情补全和收藏状态请求
- [x] 4.3 扩展 `ReaderShell` 的页码与滑块可访问文本，在详情页显示“详情”和 100% 终点状态，图片页及本地阅读器继续显示现有数字页码
- [x] 4.4 调整显示模式切换协调逻辑，使用户从详情在 scroll/single/double 之间切换后仍锚定详情，从详情返回图片后继续使用既有图片 anchor
- [x] 4.5 增加连续滚动滑块直达详情、详情向左拖回图片、可见性追踪稳定和本地阅读器进度范围不变的测试

## 5. 图片副作用与历史隔离

- [x] 5.1 将预加载目标、图片缓存和失败页统计限制在 `1..totalPages` 真实图片范围，验证进入详情不会调用 `fetchPreviewImage` 或产生详情失败页
- [x] 5.2 统一钳制防抖与关闭时的历史写入页码，确保详情停留后保存 `lastPage=totalPages` 且 `totalPages` 仍为真实图片总数
- [x] 5.3 增加详情停留后关闭/重开、初始历史页越界防护和详情页不触发重复历史写入的测试

## 6. 行为回归与验证

- [x] 6.1 增加在线详情尾页组件测试，覆盖完整字段、补全按需激活、失败重试、收藏结果、标签操作、搜索路由和打开原网页
- [x] 6.2 扩展 `ComicReaderModal` / `PageFlipView` 测试，覆盖单本、前置章节、末章、历史直达末章、三种显示模式、奇偶双页、补白组合、详情内部滚动和交互热区
- [x] 6.3 运行 `npm test -- --run tests/unit/components/ComicInfoDrawer.test.tsx tests/unit/components/common/ComicReaderModal.test.tsx tests/unit/components/LocalLibraryReaderModal.test.tsx` 及新增详情/导航测试文件
- [x] 6.4 运行 `npx tsc --noEmit`、`npm run lint` 和 `npm run lint:test-quality`，确认类型、前端 lint 与测试质量闸门通过
- [x] 6.5 运行完整 `npm test`，确认本地漫画库阅读器和现有在线阅读器行为无回归
