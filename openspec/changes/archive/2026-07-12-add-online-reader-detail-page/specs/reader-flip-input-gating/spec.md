## ADDED Requirements

### 需求:在线阅读器翻页输入必须到达并止于详情尾页

在线阅读器的当前序列包含详情尾页时，键盘、滚轮和点击翻页输入必须能从最后一个真实图片位置进入详情，并能从详情返回最后一个真实图片位置。详情必须作为向前导航终点，且图片与详情之间的过渡必须继续遵守既有 `isFlipping` 和模式切换门控，禁止在动画期间堆积输入。

#### 场景:单页模式从末图前进到详情

- **当** 单页模式位于最后一张真实图片、翻页门控放行，用户按 ArrowRight、PageDown、空格、向下滚轮或点击右侧翻页热区
- **那么** 当前导航项必须变为详情尾页
- **并且** 该变化必须触发一次正常翻页门控周期

#### 场景:双页模式从最后 spread 前进到详情

- **当** 双页模式位于包含最后一张真实图片的最后 spread、翻页门控放行，用户执行任一向前翻页输入
- **那么** 当前导航项必须直接变为独占视口的详情尾页
- **并且** 禁止因固定 step=2 越过详情或产生越界页码

#### 场景:详情页继续前进保持终点

- **当** 当前导航项为详情尾页，用户执行向前键盘或滚轮输入
- **那么** 当前导航项必须保持详情
- **并且** 禁止调用图片加载、章节切换或写入大于详情终点的页码

#### 场景:详情页后退到最后图片位置

- **当** 当前导航项为详情尾页、门控放行，用户按 ArrowLeft、ArrowUp 或 PageUp，或通过详情提供的返回操作后退
- **那么** 单页模式必须返回最后一张真实图片，双页模式必须返回最后一个图片 spread
- **并且** 后退过渡必须继续遵守既有动画期间输入丢弃和硬上限自愈规则

#### 场景:动画期间重复输入被丢弃

- **当** 图片与详情尾页之间的 enter/exit 动画尚未完成，用户再次触发翻页输入
- **那么** 重复输入必须被既有门控丢弃
- **并且** 禁止叠加多个详情实例、跳转章节或反复改写当前导航项

#### 场景:前置章节保持既有边界翻章

- **当** 多章节漫画当前位于非末章的最后图片位置并继续向前翻页
- **那么** 系统必须继续使用既有“再次翻页进入下一章”的边界提示与门控
- **并且** 禁止把该输入路由到详情尾页
