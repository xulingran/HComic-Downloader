## 1. 阅读启动意图

- [x] 1.1 为本地阅读器会话定义 `resume | restart` 启动模式，并扩展 `useLocalReaderStore` 的打开、关闭及退场清理生命周期，确保过期退出回调不会清除新会话模式
- [x] 1.2 扩展漫画库打开阅读器回调，在打开前获取最新资产详情，并把用户选择的启动模式随资产快照传给根级本地阅读器

## 2. 详情入口交互

- [x] 2.1 更新 `LibraryAssetDetailDrawer`：无进度时显示单一“开始阅读”，有效进度时显示进度提示、“继续阅读”主按钮和“从头开始”次按钮
- [x] 2.2 处理保存章节失效和窄宽度/键盘焦点场景，禁止把不可恢复的位置呈现为有效“继续阅读”入口

## 3. 阅读器初始化

- [x] 3.1 更新 `LocalLibraryReaderModal` 初始化：`resume` 使用保存章节与页码，`restart` 对单本从第 1 页、对多章节从有序章节列表第一章第 1 页打开
- [x] 3.2 保持旧进度直到页面清单成功加载，并确认失败、重试、章节选择、关闭 flush 与重新打开时不会提前覆盖或串用会话进度

## 4. 测试与验证

- [x] 4.1 扩展详情抽屉和漫画库入口测试，覆盖无进度、有效进度、失效章节及两种启动模式的参数传递
- [x] 4.2 扩展本地阅读器及 store 测试，覆盖单本/多章节恢复、从头开始、加载失败保留旧进度和退场期间新会话隔离
- [x] 4.3 运行相关 Vitest 定向测试，并通过 `npx tsc --noEmit`、`npm test`、`npm run lint` 和 `npm run lint:test-quality`
