## 1. 抽屉结构与紧凑摘要

- [x] 1.1 将 `ComicInfoDrawer` 的 drawer 面板调整为固定顶部关闭栏、唯一可滚动的中间内容区和固定底部操作区，并把抽屉最大宽度调整到约 32rem，同时保持 `w-full` 窄窗口退化
- [x] 1.2 为 drawer 表面实现紧凑封面/摘要双列布局，处理无封面占满宽度、长标题两行限制和完整标题可访问语义
- [x] 1.3 合并来源、页数、章节、更新时间、语言、分类和原网页入口的视觉排布，并将原著、角色、制作组改为紧凑行式展示，保留所有既有点击搜索行为
- [x] 1.4 增加约 600px 高短窗口的封面、padding 和间距适配，确保内容区不会挤压顶部关闭栏或底部操作区

## 2. 固定主要操作区

- [x] 2.1 将收藏按钮及开始阅读、继续阅读、从头开始按钮迁移到 drawer 固定底部操作区，保留现有按钮名称、处理函数、loading/disabled 和来源能力门控
- [x] 2.2 验证无收藏能力、无续读信息和有续读信息三种组合下操作区排布完整且不会产生水平溢出

## 3. 标签渐进展开与动画

- [x] 3.1 为 drawer 增加默认上限为 8 的标签折叠状态，在漫画切换、抽屉失活或重新打开时重置为折叠态；reader 表面保持完整标签列表
- [x] 3.2 实现“+N 展开”和“收起”键盘可操作按钮，处理标签未超限、超限、详情补全 loading/error/retry 以及补全成功后的显示状态
- [x] 3.3 限制过长标签 chip 的展示宽度并提供完整文本提示，保留标签搜索、推荐/屏蔽四态和确认弹窗行为
- [x] 3.4 让首次打开只对实际可见标签复用集中式 20ms 错峰令牌，参与项最多 20 个；展开新增标签不得逐项长级联，reduced-motion 下必须直接出现
- [x] 3.5 更新 `src/lib/anim.ts` 中已过时的 40 项注释或相关常量，使实现、注释和修订后的 `ui-animation` 规范一致

## 4. 自动化测试

- [x] 4.1 扩展 `tests/unit/components/ComicInfoDrawer.test.tsx`，验证 drawer 紧凑摘要结构、无封面退化、固定内容/操作分区和短窗口适配标记
- [x] 4.2 增加标签未超限、`+N` 计数、展开/收起、漫画切换重置、补全成功后折叠以及 reduced-motion 行为测试
- [x] 4.3 回归作者、分类、关联项和标签搜索，标签推荐/屏蔽四态，收藏状态，以及开始阅读/继续阅读/从头开始参数语义
- [x] 4.4 增加 reader 表面隔离测试，确认其不显示 drawer 专属封面、固定操作区或标签展开控件，仍展示完整标签且面板自身不产生嵌套滚动

## 5. 验证与视觉验收

- [x] 5.1 运行 `npx vitest run tests/unit/components/ComicInfoDrawer.test.tsx`、`npx tsc --noEmit` 和针对修改文件的 ESLint 检查
- [x] 5.2 运行 `npm test`、`npm run lint` 和 `npm run lint:test-quality`，确认前端全量行为与测试质量闸门通过
- [x] 5.3 启动开发环境，在 1200×800 与 900×600 窗口下分别检查常规数据、长标题和大量标签，确认核心信息及主要操作符合首屏目标且只有中间内容区滚动
- [x] 5.4 在浅色、深色和 reduced-motion 环境检查抽屉开合、标签展开、文字截断、焦点可见性及底部操作区边界

## 6. 遮罩点击穿透回归

- [x] 6.1 打开状态下让 drawer 全屏根层明确接管指针事件，inactive 状态恢复禁用，并为遮罩增加稳定测试标识
- [x] 6.2 增加点击遮罩只关闭抽屉、不触发背景控件，以及 inactive 根层不阻塞页面的回归测试
- [x] 6.3 运行 `ComicInfoDrawer` 定向测试、TypeScript 检查、修改文件 ESLint 和 OpenSpec strict validation
