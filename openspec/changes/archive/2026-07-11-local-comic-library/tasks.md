## 1. 共享契约与持久化基础

- [x] 1.1 在 `shared/types.ts` 定义漫画库资产摘要/详情、章节、分页查询、扫描状态、页面响应、阅读进度和管理结果类型，并新增对应 Python 方法、IPC channel 与 `HcomicAPI` 签名
- [x] 1.2 新建漫画库 SQLite 存储模块，创建 `library_items`、`library_chapters`、`library_reading_progress` 和 `library_scan_state` schema、索引及向前迁移逻辑
- [x] 1.3 实现索引数据库损坏隔离与可重建初始化，确保恢复流程只操作应用数据目录而不修改漫画文件
- [x] 1.4 为 schema 创建、迁移、事务回滚、损坏重建和并发访问补充 Python 单元测试

## 2. 漫画资产发现与索引

- [x] 2.1 将现有 maintenance scanner 的格式识别、自然排序、ComicInfo 解析和路径校验整理为可由维护中心与漫画库索引共同复用的纯函数
- [x] 2.2 实现顶层 CBZ、ZIP、单本图片文件夹和多章节文件夹发现，跳过临时/隐藏/空/不支持项并拒绝符号链接逃逸
- [x] 2.3 实现 `ComicInfo.xml > download_history > 文件名 > 展示兜底` 元数据合并及 ZIP/文件夹索引覆盖层，保留字段来源信息
- [x] 2.4 实现 `LibraryIndexer` 的发现、解析、批量提交和最终对账阶段，以及有界解析并发、单扫描互斥、进度事件和安全取消
- [x] 2.5 实现基于规范化相对路径、格式、大小和 mtime 的增量判定，未变化资产复用 ID/元数据，应用内重命名保留资产 ID
- [x] 2.6 实现分页查询、稳定次级排序、标题/作者/标签搜索以及来源/格式/健康状态筛选
- [x] 2.7 为混合格式、章节聚合、未关联资产、丢失历史、元数据冲突、增量刷新、取消不删除和大目录分页补充决定性 Python 测试

## 3. 漫画库 IPC 与生命周期集成

- [x] 3.1 新建 Python 漫画库 IPC mixin，接入索引扫描/取消/状态、分页列表、资产详情和章节清单方法，并注册到 `IPCServer`
- [x] 3.2 在 Electron 主进程、preload 和 React hooks 中映射新通道，逐层校验资产 ID、分页、查询长度、筛选枚举、页码和版本令牌
- [x] 3.3 增加独立的漫画库扫描进度通知，保证 stdout 实时 flush 且不覆盖下载或维护进度状态
- [x] 3.4 在下载最终产物成功落盘后触发单路径增量索引；下载未完成和临时目录阶段禁止入库
- [x] 3.5 在下载目录迁移成功后切换 root generation、使旧令牌失效并触发完整重建；迁移失败/取消时保持旧 generation
- [x] 3.6 扩展 IPC 一致性、arity、preload 暴露和 Python contract 测试，覆盖全部新增方法与通知

## 4. 本地封面与页面交付

- [x] 4.1 新建有界 `library_image_cache`，实现内容哈希命名、同目录临时文件原子写入、LRU 清理和资产版本失效
- [x] 4.2 实现按需封面提取：从 CBZ/ZIP/文件夹读取第一张有效图片并生成受限缓存结果，限制并发且支持损坏封面兜底
- [x] 4.3 实现章节/页面 manifest 的自然排序生成与版本缓存，以及按资产 ID、章节 ID、页码、版本令牌物化单页图片
- [x] 4.4 扩展后端缓存目录响应和 Electron `app-image://` resolver，仅允许 `library/<sha256>` 访问声明的漫画页缓存目录
- [x] 4.5 为压缩包条目读取、文件夹页读取、越界页、stale version、符号链接逃逸、缓存失效、LRU 和协议路径遍历补充测试

## 5. 漫画库工作区与目录界面

- [x] 5.1 将现有 `DownloadPage` 拆为工作区容器和 `DownloadTasksView`，侧边栏显示“漫画库”，默认子页签为漫画库并完整保留现有任务逻辑
- [x] 5.2 创建 `useLibraryStore`，管理分页结果、查询条件、网格/列表模式、扫描状态、选中资产、会话内滚动位置及请求竞态取消
- [x] 5.3 实现漫画库头部统计、关键字搜索、来源/格式/健康筛选、排序、刷新/取消扫描和网格/列表切换
- [x] 5.4 实现漫画卡片、按需封面、SFW 占位、骨架屏、分页/虚拟化边界、空目录、扫描中、扫描失败及部分元数据状态
- [x] 5.5 实现资产详情抽屉，展示标题、作者、标签、来源、格式、页数、大小、路径摘要、章节、健康摘要和可用操作
- [x] 5.6 在“下载任务”页签使用 `ACTIVE_DOWNLOAD_STATUSES` 显示活跃数量，并保证用户浏览漫画库时下载进度仍持续更新
- [x] 5.7 补充工作区默认页签、子页签保活、下载任务行为回归、查询竞态、筛选排序、封面状态和响应式布局前端测试

## 6. 本地漫画阅读

- [x] 6.1 将现有阅读器的数据获取抽象为 `remote-preview` 与 `local-library` reader source，保持远程预览接口和行为不变
- [x] 6.2 实现从漫画库卡片/详情打开本地单本或专辑、选择章节、获取页 URL，并在 source/资产版本/章节变化时清空预加载缓存
- [x] 6.3 实现本地阅读进度的节流保存、关闭/切章前 flush、继续阅读、最近阅读排序和页数缩短时的边界收敛
- [x] 6.4 处理资产外部修改、删除、stale version、损坏单页和缓存 404，停止旧预加载并提供重新索引、重试或跳过操作
- [x] 6.5 补充 CBZ/ZIP/文件夹/多章节阅读、自然页序、进度恢复、切章缓存失效、资产变化以及远程阅读无回归测试

## 7. 安全资产管理

- [x] 7.1 实现统一资产 mutation guard：按 asset ID + expected version 重新解析真实路径，验证当前 root generation，并检查下载/打包/迁移/扫描/体检/编辑/阅读冲突
- [x] 7.2 实现“在文件管理器中显示”：Python 权威解析路径后由 Electron 主进程调用 `shell.showItemInFolder` 或 `shell.openPath`，路径不暴露给任意渲染文件 API
- [x] 7.3 为漫画库单项健康检查接入现有 `HealthChecker` 和共享页数/图片检查逻辑，支持没有下载历史的本地资产
- [x] 7.4 实现删除准备令牌、TTL/一次性校验、Electron `shell.trashItem` 和成功后的索引/manifest/缓存/精确下载历史清理
- [x] 7.5 实现同根目录安全重命名、文件名/保留名/冲突校验、索引与下载历史事务更新，以及数据库失败时的文件名回滚或单项对账
- [x] 7.6 实现 CBZ 元数据编辑的同目录临时归档、条目保留、唯一 ComicInfo 替换、`testzip`/页数校验和 `os.replace`，并实现 ZIP/文件夹仅索引覆盖值
- [x] 7.7 为 TOCTOU 占用、旧 generation、回收站失败、提交失败、目标冲突、重命名回滚、磁盘空间不足、CBZ 重写中断和符号链接逃逸补充失败注入测试

## 8. 集成收尾与质量闸门

- [x] 8.1 串联下载完成、完整/增量扫描、卡片更新、阅读进度、管理操作和目录迁移的端到端状态刷新，消除重复扫描和陈旧 UI
- [x] 8.2 检查日志与错误文案不泄露超出用户已配置根目录的路径或压缩包内部敏感信息，并补充诊断字段和可恢复操作提示
- [x] 8.3 更新相关 OpenSpec 交叉引用和用户文档，说明支持格式、首次扫描、索引可重建、回收站删除、元数据写回差异和单根目录限制
- [x] 8.4 运行 `pytest`，修复全部 Python 回归
- [x] 8.5 运行 `npx tsc --noEmit` 与 `npm test`，修复全部 TypeScript 类型和前端回归
- [x] 8.6 运行 `npm run lint:py`、`npm run format:py`、`npm run lint` 和 `npm run lint:test-quality`，修复全部质量闸门问题
- [x] 8.7 运行 `openspec-cn validate local-comic-library --strict`，确认 proposal、四份 capability specs、design 和 tasks 全部可归档且实现追踪完整
